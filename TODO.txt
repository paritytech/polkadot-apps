PR #20 Review Comments — TODO (after PR #21 merge)
=====================================================

ALREADY ADDRESSED (by PR #21 merge or prior commits):
- registry.ts comments — file deleted, replaced by descriptors approach
- "Should we resetRegistry() in a beforeEach hook?" — registry gone
- "We should have these tests" (clients.ts) — test coverage added
- "can this be Promise<InkSdk>?" — createInkSdk is now a static import, type derived directly
- "Let's keep the descriptors" / "merge descriptors PR here?" — done
- "Let's add some version" (package.json) — version is 0.1.5

REMAINING:

1. [providers.ts:17] Fallback created preemptively
   @charlesHetterich: "Shouldn't we only create the fallback if we end up in the
   catch? instead of always making it preemptively"
   Currently createFallbackProvider runs before trying product-sdk. Should only
   build fallback if product-sdk is unavailable.

2. [providers.ts:23] Catch swallows runtime errors from createPapiProvider
   @UtkarshBhardwaj007: The catch block catches both "module not found" AND
   runtime errors from createPapiProvider(). These should be separated — only
   catch import failures, let runtime errors propagate.

3. [providers.ts:91] fetchChainSpec accepts malformed JSON
   @UtkarshBhardwaj007: "This can return for malformed inputs starting with '{'
   as well. Maybe JSON.parse in a try/catch?"

4. [providers.ts:54] smoldotInstance not reset by destroyAll()
   @UtkarshBhardwaj007: "destroyAll() doesn't reset this singleton — the smoldot
   worker keeps running."

5. [providers.ts:69] Relay chain cached per parachain (duplicate addChain)
   @UtkarshBhardwaj007: "If two parachains share a relay, the relay gets
   addChain() twice — can we cache relay chain?"

6. [providers.ts:100] Missing test coverage for providers
   @charlesHetterich + @UtkarshBhardwaj007: Need tests for product-sdk wrapping
   path, fallback path, and sdk-not-installed path.

7. [container.ts:14] require() in ESM module
   @UtkarshBhardwaj007: "require() in an ESM module. Can we use import like
   providers.ts"

8. [container.ts:13] eslint-disable comment — package uses biome
   @UtkarshBhardwaj007: eslint-disable comment is irrelevant, package uses biome.

9. [hmr.ts:4] eslint-disable comment — package uses biome
   @UtkarshBhardwaj007: Same issue, eslint-disable comment should be removed.

10. [docs/plans/chain-client.md] Should be removed before merging
    @charlesHetterich: "this file should be removed before merging into main"

11. [testing.ts] "This file isn't used anywhere, I would delete this entirely"
    @charlesHetterich: testing.ts may not be needed. However it currently exports
    reset() which calls destroyAll() — evaluate if this is still useful or if
    consumers should just use destroyAll() directly.
