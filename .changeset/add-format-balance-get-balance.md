---
"@polkadot-apps/utils": minor
---

Add `formatBalance` for locale-aware display formatting with thousand separators, decimal truncation, and optional token symbol. Add `getBalance` typed convenience wrapper for System.Account balance queries.
